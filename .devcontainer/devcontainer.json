{
  "name": "C Development",
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  // ターミナルと postCreateCommand を root 権限で動かす設定
  "remoteUser": "root",
  "settings": {
    "terminal.integrated.shell.linux": "/bin/sh"
  },
  "extensions": [
    "ms-vscode.cpptools",
    "twxs.cmake",
    "wraith-gitlens"
  ],
  "postCreateCommand": "apk update && apk add --no-cache build-base gdb clang clang-dev clang-tools-extra cmake git git-lfs cunit catch2"
}
